<Window x:Class="PL.Volunteer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="400" Background="#F0F8FF" Closed="AdminWindow_Closed">
    <Window.DataContext>
        <local:MainWindowVM />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Welcome!" 
                   FontSize="20" 
                   FontWeight="Bold"
                   Foreground="#2E8B57"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"
                   Grid.Row="0"/>

        <!-- Label for ID -->
        <TextBlock Text="Enter your ID:" 
                   FontSize="14"
                   Foreground="#2F4F4F"
                   HorizontalAlignment="Left"
                   Margin="0,10,0,5"
                   Grid.Row="1" />

        <!-- TextBox for ID -->
        <TextBox Grid.Row="2"
                 Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}" 
                 HorizontalAlignment="Stretch" 
                 Width="300" 
                 Margin="0,0,0,10" 
                 HorizontalContentAlignment="Center"
                 VerticalContentAlignment="Center"
                 Background="#FFFFFF" 
                 BorderBrush="#4682B4" />

        <!-- Label for Password -->
        <TextBlock Grid.Row="3"
                   Text="Enter your password:" 
                   FontSize="14"
                   Foreground="#2F4F4F"
                   HorizontalAlignment="Left" 
                   Margin="0,10,0,5"/>

        <!-- PasswordBox for Password -->
        <PasswordBox Grid.Row="4"
                     Margin="30,0,30,214"
                     Background="#FFFFFF"
                     BorderBrush="#4682B4"
                     HorizontalContentAlignment="Center"
                     VerticalContentAlignment="Center"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

        <!-- Login Button -->
        <Button Content="Login" 
                Grid.Row="4" 
                Width="300"
                HorizontalAlignment="Center"
                Background="#4682B4" 
                Foreground="White" 
                FontSize="16"
                FontWeight="Bold"
                Margin="0,158,0,34"
                Click="Login_Click" RenderTransformOrigin="0.5,0.5" >
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform ScaleY="1"/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform/>
                </TransformGroup>
            </Button.RenderTransform>
        </Button>

        <!-- Error Message -->
        <TextBlock Text="{Binding ErrorMessage}" 
                   Foreground="Red" 
                   Visibility="{Binding ErrorVisibility}" 
                   HorizontalAlignment="Center"
                   FontSize="12"
                   Grid.Row="6" />
    </Grid>
</Window>
