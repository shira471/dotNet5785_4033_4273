<Window x:Class="PL.Volunteer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBox x:Name="txtId"  HorizontalAlignment="Center" Margin="0,64,0,0" Grid.Row="2" TextWrapping="Wrap" Text="enter your ID:" VerticalAlignment="Top" Width="120"/>

        <!-- שדה סיסמה -->
        <TextBox x:Name="txtPassword"  HorizontalAlignment="Center" Margin="0,87,0,0" Grid.Row="2" TextWrapping="Wrap" Text="enter your password:" VerticalAlignment="Top" Width="120"/>

        <!-- כפתור התחברות -->
        <Button Content="Login" Click="Login_Click" Margin="0,179,0,3" Grid.Row="2" />

        <!-- תצוגת שגיאה -->
        <TextBlock x:Name="lblError" Foreground="Red" Visibility="Collapsed" />
    </Grid>
</Window>
