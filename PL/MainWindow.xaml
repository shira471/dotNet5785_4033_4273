<Window x:Class="PL.Volunteer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainWindowVM />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- TextBox for UserId -->
        <TextBox HorizontalAlignment="Center" Margin="0,64,0,0" Grid.Row="0" 
                 Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}" 
                 VerticalAlignment="Top" Width="120" />

        <!-- PasswordBox for Password -->
        <PasswordBox HorizontalAlignment="Center" Margin="0,87,0,0" Grid.Row="1" Width="120"
                     PasswordChanged="PasswordBox_PasswordChanged" />

        <!-- Login Button -->
        <Button Content="Login" Click="Login_Click" Margin="0,179,0,3" Grid.Row="2" />

        <!-- Error Message -->
        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" 
                   Visibility="{Binding ErrorVisibility}" />
    </Grid>
</Window>
