<Window x:Class="PL.Calls.SelectCallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        DataContext="{Binding Vm, RelativeSource={RelativeSource Mode= Self}}"
        Title="Select Call Window"
        Height="640"
        Width="800"
        Background="Lavender"
        FontFamily="Segoe UI">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- כותרת -->
        <TextBlock Text="Select a Call" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="DarkSlateBlue" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,10" 
                   Grid.Row="0" />

        <!-- Filter Section -->
        <StackPanel Orientation="Horizontal" Margin="5" Grid.Row="1" VerticalAlignment="Center">
            <!-- Filter -->
            <TextBlock Text="Filter by Type:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0" />
            <ComboBox Width="200"
                      ItemsSource="{Binding CallTypes}"
                      SelectedItem="{Binding FilterCallType}"
                      Margin="0,0,10,0" />

            <!-- Sort -->
            <TextBlock Text="Sort By:" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0,10,0" />
            <ComboBox Width="200"
                      ItemsSource="{Binding OpenCallSortField}"
                      SelectedItem="{Binding SortField}"
                      Margin="0,0,10,0" />
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Calls}" 
                  SelectedItem="{Binding Selected, Mode=TwoWay}" 
                  AutoGenerateColumns="False" 
                  CanUserSortColumns="True" 
                  Margin="5" 
                  BorderBrush="Gray" 
                  BorderThickness="1" 
                  Background="White" 
                  AlternatingRowBackground="LightGray" 
                  RowHeaderWidth="0"
                  IsReadOnly="True"
                   MouseDoubleClick="CallsDataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*" />
                <DataGridTextColumn Header="Call Type" Binding="{Binding Tkoc}" Width="*" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="3*" />
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="2*" />
                <DataGridTextColumn Header="Open Time" Binding="{Binding OpenTime}" Width="2*" />
                <DataGridTextColumn Header="End Time" Binding="{Binding MaxEndTime}" Width="2*" />
                <DataGridTextColumn Header="Distance" Binding="{Binding DistanceFromVolunteer}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="End call" 
                    Width="100" 
                    Height="30" 
                    Background="LightSkyBlue" 
                    Foreground="DarkBlue" 
                    FontWeight="Bold" 
                    Margin="5" 
                    Click="FinishCall_Click" />
            <!--<Button Content="Select call" 
                    Width="100" 
                    Height="30" 
                    Background="LightSkyBlue" 
                    Foreground="DarkBlue" 
                    FontWeight="Bold" 
                    Margin="5" 
                    Click="SelectCall_Click" />-->
            <Button Content="Cancel call" 
                    Width="100" 
                    Height="30" 
                    Background="LightSkyBlue" 
                    Foreground="DarkBlue" 
                    FontWeight="Bold" 
                    Margin="5" 
                    Click="btnCanceled_Click" />
            <Button Content="Back" Width="150" Margin="5" Click="btnBack_Click" Background="LightBlue" Foreground="DarkBlue" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>